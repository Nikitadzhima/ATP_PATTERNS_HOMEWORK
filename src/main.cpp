#include "include/Field.h"
#include "include/Cards_storage.h"

int main() {
    Game_settings* game_settings = Game_settings::get_instance(); // create settings
    // create field(empty)
    // create deck(full deck)
    // create hand(empty)
    // create discard(empty)
}
